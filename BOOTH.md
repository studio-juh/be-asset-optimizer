# BOOTH掲載用テキスト — Be Asset Optimizer 0.6.1

以下の「商品名」「短い紹介文」「商品説明」はBOOTHの商品ページへ貼り付けて使用できます。公開前チェックは掲載文に含めません。

## 商品名

【Windows】Be Asset Optimizer｜Blender向け画像・3Dアセット最適化

## 短い紹介文

Blenderへ持ち込む前の画像を、まとめて軽量化・リサイズ・減色。ノーマルマップ、テクスチャアトラス、AI復元、FBX / GLBプレビューも備えたWindows用デスクトップツールです。

## 商品説明

Be Asset Optimizerは、Blenderで使う画像・テクスチャ・3Dアセットをまとめて整えるWindows向けデスクトップアプリです。

画像やフォルダーをドラッグ＆ドロップし、複数ファイルをリストで一括処理できます。変換はPC内で完結し、入力した素材そのものを外部サービスへ送信しません。

### できること

■ 画像最適化

- PNG / WebP / JPEG出力
- 複数画像・フォルダーの一括処理
- 最大幅・最大高・倍率を指定したリサイズ
- PNG-8（16〜256色）、PNG-24、PNG-32、グレースケール
- ディザリングと圧縮強度の調整
- 変換前後の寸法・容量・状態を一覧表示

■ AI復元

- Real-ESRGAN / RealESRNetによるローカル処理
- 元の寸法 / 2倍 / 4倍出力
- 自然さ重視 / 高精細モード
- 復元強度、分割品質、境界ブレンドの調整
- 必要なAI機能だけを後から追加

■ ノーマルマップ

- 高さマップや画像の明度から一括生成
- Blender向けOpenGL（+Y）/ DirectX（-Y）切り替え
- 強さ、反転、長辺リサイズ、正方形補完

■ テクスチャアトラス

- 4枚を2×2アトラスへ結合
- 任意の枠へのドロップとドラッグ入れ替え
- 正方形補完とセル解像度指定

■ 3Dプレビュー

- FBX / GLBの簡易表示
- FBXの相対テクスチャ探索
- GLB埋め込み画像、アニメーション、ワイヤーフレーム表示
- 自動回転と回転リセット

### 対応形式

画像入力: PNG / JPEG / JFIF / WebP / HEIC / HEIF / BMP / TIFF / GIF / TGA / DDS

画像出力: PNG / WebP / JPEG

3Dプレビュー: FBX / GLB

HEIC / HEIFはWindowsの追加コーデックなしで読み込めます。GIFは先頭フレームを静止画として使用します。

### 動作環境

- Windows 10 / 11 64-bit
- Microsoft Edge WebView2 Runtime
- 960 × 640以上の画面
- AI復元のみ: Vulkan対応GPU、最新のGPUドライバー
- AI機能の初回追加時のみインターネット接続と約70 MBの空き容量

AI復元以外の機能はAIモデルや対応GPUなしで利用できます。

### 配布ファイル

- セットアップ版: `Be_Asset_Optimizer_0.6.1_BOOTH_Setup.zip`（インストーラー、README、ライセンスを同梱）
- ポータブル版: `Be_Asset_Optimizer_0.6.1_windows_x64_portable.zip`

通常はセットアップ版がおすすめです。インストールせず試したい場合やUSBメモリ等で持ち運ぶ場合はポータブル版を使用してください。両方の機能は同じです。

### AI機能について

AI復元を初めて使うときに、アプリ内から約61 MBのAIコンポーネントを取得します。4分割で並列取得し、ZIPと各ファイルのSHA-256が一致した場合だけ保存します。追加後の復元処理はPC内で実行され、画像はアップロードされません。

AI処理は元画像にない細部を推測するため、画像によっては模様・ぼかし・タイル状の違和感が生じる場合があります。ノーマル、粗さ、金属、マスク画像への直接使用は推奨しません。

### ご注意

- Windows x64専用です。macOS / Linux版はありません。
- 現在の配布物にはコード署名がないため、Windowsの発行元確認やSmartScreenが表示される場合があります。入手元とファイル名を確認して実行してください。
- 3Dプレビューは確認用です。モデルの編集や再出力は行いません。
- 元ファイルは上書きしませんが、重要な素材は事前にバックアップしてください。
- 本ソフトウェアは無保証で提供されます。

### ライセンス・ソースコード

本アプリはGPL-3.0-or-laterで公開しているオープンソースソフトウェアです。ライセンスの範囲で利用・改変・再配布できます。

ソースコード:
https://github.com/studio-juh/be-asset-optimizer

AIコンポーネント:
https://github.com/studio-juh/be-asset-optimizer/releases/tag/ai-components-v1.0.0

不具合報告:
https://github.com/studio-juh/be-asset-optimizer/issues

## 検索タグ候補

`Blender` `3D` `テクスチャ` `画像圧縮` `画像最適化` `ノーマルマップ` `テクスチャアトラス` `FBX` `GLB` `Windows` `ツール` `OSS`

## 商品画像の構成案

1. アプリ全体と「Blender向け画像・3Dアセット最適化」の一文
2. 複数画像を並べた画像最適化画面
3. 変換前後の容量比較
4. ノーマルマップ生成画面
5. 2×2テクスチャアトラス画面
6. FBX / GLBプレビュー画面
7. AI復元は任意追加・ローカル処理であることの説明

## BOOTHへ登録するファイル

推奨する登録内容です。

1. セットアップ版を含むZIP
2. ポータブル版ZIP
3. `README.txt`（必要に応じて単体でも登録）

セットアップ版ZIPには、セットアップEXEに加えて `README.txt`、`LICENSE`、`THIRD_PARTY_NOTICES.md` を同梱します。MSI版は一般利用者には用途が重複するため、通常は登録不要です。

## 公開前チェック

- [ ] BOOTHへ載せるバージョンとファイル名が一致している
- [ ] セットアップ版とポータブル版を別のWindowsユーザー環境で起動できる
- [ ] Windowsの警告表示について商品説明へ記載した
- [ ] AI機能の追加、キャンセル、削除、再追加を確認した
- [ ] AIコンポーネントを削除した状態で通常機能が使える
- [ ] 公開バイナリと同じソースをGitHubへpushした
- [ ] `v0.6.1` タグまたはReleaseから対応ソースを取得できる
- [ ] `LICENSE` と `THIRD_PARTY_NOTICES.md` を配布物へ同梱した
- [ ] 配布ファイルのSHA-256を控えた
- [ ] 商品ページのスクリーンショットに個人情報やローカルパスが写っていない

## 頒布価格について

GPLでは有料での頒布も可能ですが、入手した利用者にはGPLに基づく改変・再配布の権利があります。支援価格を設定する場合は、独占ライセンスの販売ではなく、開発・配布・更新を支援するための頒布であることが伝わる説明が適しています。
