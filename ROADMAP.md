# Be Asset Optimizer MVP 工程表

最終更新: 2026-08-18  
対象: Windows を最初の配布先とする、ローカル完結型の一括 PNG 軽量化デスクトップアプリ

## MVP の到達点

利用者が複数の PNG をドラッグ＆ドロップし、リストで状態と削減量を確認しながら、リサイズ・インデックスカラー化・最適化を一括実行できること。

### MVP に含めること

- PNG ファイルのドラッグ＆ドロップとファイル選択
- キュー形式の一覧（サムネイル、寸法、サイズ、処理状態、削減率）
- 元画像を拡大しない、縦横比を維持したリサイズ
- `高品質 (Lanczos3)` と `ピクセルアート (Nearest)` のリサイズ方式
- インデックスカラー化（オフ、256、128、64、32、16 色）とディザリングのオン／オフ
- `oxipng` による最終最適化
- 出力先の選択、同名衝突回避、元ファイルを上書きしない既定値
- 個別の成功／失敗／スキップ、全体進捗、合計削減量
- 出力が元より大きい場合は元画像を採用してスキップと記録

### MVP では扱わないこと

- JPG / WebP / AVIF 入出力
- フォルダ監視、CLI、自動アップデート
- APNG、16-bit PNG、ICC プロファイル付き PNG の変換
- 任意トリミング、余白追加、目標ファイルサイズへの自動探索
- macOS / Linux の配布

これらはエラーではなく「未対応として安全にスキップ」する。

## 技術方針

| 層 | 採用 | 担当 |
| --- | --- | --- |
| デスクトップ基盤 | Tauri 2 | ウィンドウ、D&D、ネイティブ配布 |
| UI | React + TypeScript | キュー、設定、結果表示 |
| 画像処理 | Rust | パス検証、キュー、出力、進捗 |
| リサイズ | image crate（MVP） | Lanczos3 / Nearest、透過を考慮。大量処理の性能検証後に fast_image_resize へ置換可能 |
| 減色 | imagequant | パレット生成、透過、ディザリング |
| PNG 読書き | png crate | RGBA 読込、Indexed PNG 書出し |
| 最終圧縮 | oxipng | 可逆最適化、不要メタデータ整理 |

アプリと配布物は `GPL-3.0-or-later` とし、`imagequant` のライセンス表示を同梱する。

## 処理パイプライン

```text
入力 PNG
  → 検査（PNG / 非APNG / 8-bit / sRGB 相当）
  → RGBA へ読込
  → リサイズ（有効時）
  → インデックスカラー化（有効時）
  → PNG 書出し
  → oxipng
  → 元ファイルとのサイズ比較
  → 出力または「小さくならなかったためスキップ」
```

ファイル単位の並列数は初期値を `2` とする。画像処理ライブラリ内部の並列化と競合させないため、無制限の同時実行はしない。

## 工程

### 0. 基盤と仕様固定

- Tauri 2 + React + TypeScript プロジェクトを作成する。
- GPL-3.0-or-later の `LICENSE` と依存ライセンス表示の置き場を作る。
- Rust 側に `Job`、`JobStatus`、`Settings`、`ProcessResult` の型を定義する。
- 入力・出力パスのルールを固定する。

完了条件: 空の Tauri アプリが Windows で起動し、Rust コマンドを UI から呼べる。

### 1. 入力キューと一覧 UI

- ファイル D&D とファイル選択を実装する。
- PNG だけを受け付け、重複を除外する。
- ファイル名、元サイズ、寸法、サムネイルを一覧表示する。
- 行の削除、全件クリア、処理対象の選択を実装する。

完了条件: 100 枚を追加しても UI が操作可能で、非 PNG は明確に拒否される。

### 2. 安全な出力と可逆最適化

- 出力先選択と既定の `optimized` フォルダを実装する。
- 同名衝突時に連番を付ける。
- `oxipng` の安全なプリセットを組み込む。
- 元より大きい出力を保存しない判定を実装する。
- 行単位の進捗、成功、失敗、スキップを UI に送る。

完了条件: 元ファイルを変更せず、複数ファイルの結果と合計削減量を表示できる。

### 3. リサイズ

- 最大幅・最大高・倍率の設定を実装する。
- 縦横比を維持し、既定では拡大しない。
- `Lanczos3` と `Nearest` を選べるようにする。
- 透過境界に色フリンジが出ないことをテストする。

完了条件: 写真系 PNG とピクセルアート PNG が意図した寸法・方式で出力される。

### 4. インデックスカラー化

- 色数プリセットとディザリング設定を実装する。
- `imagequant` の品質エラーをジョブ結果として記録する。
- Indexed PNG（PLTE / tRNS）を書き出し、続けて `oxipng` を実行する。
- 既に小さい PNG では元を残す判定を確認する。

完了条件: 透過 PNG を含む各プリセットで、色数とファイルサイズが期待どおり反映される。

### 5. 仕上げと配布検証

- キャンセル、失敗のみ再試行、完了ファイルをエクスプローラーで開く機能を追加する。
- 設定の永続化を実装する。
- 空入力、壊れた PNG、巨大画像、読み取り専用出力先を検証する。
- Windows インストーラーをビルドし、クリーン環境で動作確認する。
- OSS ライセンスと第三者ライセンス表示を配布物に含める。

完了条件: 新規環境でインストールから 100 枚の処理、結果確認まで完了できる。

## 受け入れテスト

1. 100 枚の PNG をドロップすると、すべて一覧に入り、アプリが固まらない。
2. `安全` プリセットは画素を変更せず、元より大きい出力を保存しない。
3. 1280 px の最大幅を指定した場合、縦横比を維持して縮小され、拡大はされない。
4. `Nearest` はピクセルアートの輪郭をぼかさない。
5. 64 色・ディザリング有効では、透過を残した Indexed PNG が生成される。
6. 壊れた PNG は他のジョブを止めず、対象行に理由を表示する。
7. 元ファイルは、上書きを明示的に有効にしない限り変更されない。

## MVP 完了後の候補

- Blender 用ノーマルマップの一括生成（実装済み）
- WebP / AVIF への一括変換
- フォルダ再帰追加とフォルダ監視
- 目標サイズ（例: 300 KB 以下）に合わせた品質探索
- before / after の拡大プレビューと差分表示
- macOS / Linux 向け配布
